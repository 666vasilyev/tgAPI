# Telegram's comments grabbing microservice

## Описание

Данный микросервис построен на **FastAPI**, с использованием **Celery**, **Telethon** и сопутствующих инструментов, таких как **Docker**, **nginx**. В качестве брокера был использован **Redis**, для backend **PostgreSQL**.<br />

### Для получения актуальной документации по взаимодействию с сервисом, запустите проект и перейдите по [ссылке](http://localhost:8000/redoc)

## Получение активной сессии telethon

Для получения тестовой сессии по имеющимся персональным данным (номер телефона\id бота) необходимо запустить файл `get_session.py`, 
выполнить вход в аккаунт и добавить его по endpoint `POST /account` с помощью [docs](http://localhost:8000/docs), либо с помощью 
CURL запроса 
```shell
curl -X 'POST' \
  'http://localhost:8145/account' \
  -H 'accept: application/json' \
  -H 'Content-Type: multipart/form-data' \
  -F 'session_file=@user_number.session'
  ```

## Описание алгоритма.

![1](https://user-images.githubusercontent.com/83702683/226186918-982655c6-962d-468d-a9f4-e74ca18213b3.jpg)
![2](https://user-images.githubusercontent.com/83702683/226186933-ea708e16-ae55-4f5d-b6a0-a01f612644c1.jpg)

## CHANGELOG

### 6.04.2023
 - Был проведен code review
### 7.04.2023
 - Прокси прописывается в базе через роут `add_proxy`
 - task_id был убрал из запроса с созданием задач
 - Доработана логика работы с прокси
### 11.04.2023
 - Убраны ${POSTGRES_PORT}:${POSTGRES_PORT} из `docker-compose.yaml`
 - Восстановлена последовательность миграция `alembic`
### 19.04.2023
 - Исправлен `docker-compose.yaml`
### 4.05.2023
 - При добавлении нового аккаунта появилась возможность сразу же задавать для него прокси. 
 - Исправлена логика добавления аккаунта в случае, когда аккаунт получил бан на стороне Telegram
### 14.05.2023
 - Добавлен роут account_post_with_proxy который добавляет аккаунт и вместе с ним прокси
 - Исправлен роут post /account, теперь аккаунт создается при заданном proxy_id
 - Исправлен put /account, теперь можно по отдельности внести изменения в имеющийся аккаунт как файла сессии так и прокс ид 
 - Появился changelog
### 20.05.2023
 - Добавлено поле requests в db accounts
 - Сделана функция подсчета запросов на аккаунт
 - Сделана функция выбора аккаунта с наименьшим числом запросов
 - Сделан небольшой рефакторинг
 ### 12.09.2023
 - Сделан рефакторинг кода
 - Упрощена логика в файле worker.py
 - Из запросов убрана работа с базой данных, перенесена в отдельные функции
 - Работа с базой данных стала синхронной(для упрощения получения сессий работы с базой данных и их отслеживания)
 - Изменен принцип получения сессий базы данных(подробнее см.файл `connection.py`)
 - База данных переведена с PostgreSQL на SQLite
 - Изменнены модели базы данных и формат хранения данных
 - В `docker compose` стало меньше контейнеров
 - Добавлена конвертация данных из базы данных SQLite в MS Excel, по заданному формату
 - Добавлены два роута: `get /report`(beta) & `post /report`, позволяющие сформировать отчет в Excel формате
 - Добавлена обработка media с постов, теперь они хранятся в директории `~/tgAPI2.0/media`
 - Добавлены комментарии к коду и логи
 ### 13.09.2023
 - Исправлен Dockerfile и docker-compose.yaml
 ### 17.09.2023
 - Исправлен Dockerfile и docker-compose.yaml
 - Обработаны некоторые ошибки